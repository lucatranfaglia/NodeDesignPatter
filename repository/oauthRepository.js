import * as dotenv from 'dotenv';
dotenv.config();


/**
 * Get file spec to paint the jgv graph
 * return: file spec.json from S3
 */
export const oauth = async function () {  
    try {                 
        return 1        
    }
    catch (error) {            
        throw new Error(error);
    }
}


export default oauth;